    RewriteCond %{HTTP:Connection} Upgrade [NC]
    RewriteCond %{HTTP:Upgrade} websocket [NC]
    RewriteRule /$teacherId.* ws://localhost:$privateWsPort  [P,L]

    ProxyPass /$teacherId http://localhost:$privateHttpPort/$teacherId
    ProxyPassReverse /$teacherId http://localhost:$privateHttpPort/$teacherId
